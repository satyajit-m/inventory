<div class="card">
  <h1 class="card-header text-light bg-info">Editing Category</h1>

  <%= render 'form', category: @category %>
</div>

<%= link_to 'Show', @category %> |
<%= link_to 'Back', categories_path %>